{
  "name": "debe-session-reschedule",
  "version": "1.0.0",
  "private": true,
  "description": "Part 3 — parent-facing session reschedule widget. npm workspaces so the frontend and the Cloud Function import the same types from one package.",
  "workspaces": [
    "shared",
    "web",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "//build": "shared/ has no build step — it ships TypeScript source, which Next transpiles directly. The Cloud Function is the one consumer that needs a real artefact; see build:functions.",
    "build": "npm run build --workspace web",
    "build:functions": "npm run build --workspace functions",
    "start": "npm run start --workspace web",
    "lint": "npm run lint --workspace web",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run typecheck && npm run lint && npm run test"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
