{
  "name": "@debe/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Types, time arithmetic, scheduling policy and the reschedule validator. Imported by both the Next.js app and the Cloud Function so the contract cannot drift.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
