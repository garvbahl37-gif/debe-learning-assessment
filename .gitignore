node_modules/
.next/
out/
build/
dist/

# The Cloud Function's esbuild output. Scoped to the exact path on purpose —
# a bare `lib/` here also matches web/src/lib/, which silently dropped three
# source files from the repo and broke a fresh clone.
part3-widget/functions/lib/

*.log
npm-debug.log*
.DS_Store

.env
.env*.local

coverage/
.vercel
next-env.d.ts
.turbo/

# Firebase emulator artefacts
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Next.js writes these on dev start; disabled via agentRules in next.config
AGENTS.md
CLAUDE.md
